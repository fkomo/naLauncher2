<Window x:Class="naLauncher2.Wpf.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:naLauncher2.Wpf"
    mc:Ignorable="d"
    Title="MainWindow"
    WindowStyle="None"
    WindowState="Maximized"
    AllowsTransparency="True"
    Background="#00202020"
    KeyDown="Window_KeyDown"
    Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="DropdownItemStyle" TargetType="TextBlock">
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3A3F42"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid x:Name="RootGrid" Background="#F0181D1F">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- New Games (horizontal strip) -->
        <StackPanel Grid.Row="0" Orientation="Vertical">
            <Grid Margin="32,32,32,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Path x:Name="NewGamesToggle"
                          Data="M 0,0 H 10 V 10 H 0 Z M 2,5 H 8 M 5,2 V 8"
                          Stroke="White" StrokeThickness="1.3" Fill="Transparent"
                          Width="12" Height="12" Stretch="Uniform"
                          Opacity="0.5" Cursor="Hand" VerticalAlignment="Center" Margin="0,1,10,0"
                          MouseLeftButtonUp="NewGamesToggle_Click"/>
                    <TextBlock Text="New" x:Name="NewGamesLabel" Foreground="White" Opacity="1"
                               FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="" x:Name="NewGamesCount" Foreground="White" Opacity="1"
                           FontSize="16" FontWeight="Normal"
                           VerticalAlignment="Center" Margin="8,0,16,0"/>
                <Rectangle Grid.Column="2" x:Name="NewGamesDivider" Height="1" Fill="White" Opacity="0.2" VerticalAlignment="Center" Visibility="Collapsed" Margin="0,3,0,0"/>
                <Rectangle Grid.Column="2" x:Name="NewGamesScrollTrack" Height="1" Fill="White" Opacity="0.2" VerticalAlignment="Center" Margin="0,3,0,0"/>
                <Rectangle Grid.Column="2" x:Name="NewGamesScrollThumb" Height="3" Fill="LightSkyBlue" Opacity="0" VerticalAlignment="Center" Margin="0,3,0,0" HorizontalAlignment="Left" Width="0">
                    <Rectangle.Effect>
                        <DropShadowEffect Color="LightSkyBlue" ShadowDepth="0" BlurRadius="6" Opacity="0.8"/>
                    </Rectangle.Effect>
                </Rectangle>
            </Grid>
            <Canvas x:Name="NewGamesCanvas" Height="300" ClipToBounds="True" Margin="0,0,0,0" MouseWheel="HorizontalCanvas_MouseWheel">
                <Canvas x:Name="NewGamesContainer"/>
            </Canvas>
        </StackPanel>

        <!-- Recent Games (horizontal strip) -->
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <Grid Margin="32,16,32,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Path x:Name="RecentGamesToggle"
                          Data="M 0,0 H 10 V 10 H 0 Z M 2,5 H 8 M 5,2 V 8"
                          Stroke="White" StrokeThickness="1.3" Fill="Transparent"
                          Width="12" Height="12" Stretch="Uniform"
                          Opacity="0.5" Cursor="Hand" VerticalAlignment="Center" Margin="0,1,10,0"
                          MouseLeftButtonUp="RecentGamesToggle_Click"/>
                    <TextBlock Text="Recent" x:Name="RecentGamesLabel" Foreground="White" Opacity="1"
                               FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="" x:Name="RecentGamesCount" Foreground="White" Opacity="1"
                           FontSize="16" FontWeight="Normal"
                           VerticalAlignment="Center" Margin="8,0,16,0"/>
                <Rectangle Grid.Column="2" x:Name="RecentGamesDivider" Height="1" Fill="White" Opacity="0.2" VerticalAlignment="Center" Visibility="Collapsed" Margin="0,3,0,0"/>
                <Rectangle Grid.Column="2" x:Name="RecentGamesScrollTrack" Height="1" Fill="White" Opacity="0.2" VerticalAlignment="Center" Margin="0,3,0,0"/>
                <Rectangle Grid.Column="2" x:Name="RecentGamesScrollThumb" Height="3" Fill="LightSkyBlue" Opacity="0" VerticalAlignment="Center" Margin="0,3,0,0" HorizontalAlignment="Left" Width="0">
                    <Rectangle.Effect>
                        <DropShadowEffect Color="LightSkyBlue" ShadowDepth="0" BlurRadius="6" Opacity="0.8"/>
                    </Rectangle.Effect>
                </Rectangle>
            </Grid>
            <Canvas x:Name="RecentGamesCanvas" Height="300" ClipToBounds="True" Margin="0,0,0,0" MouseWheel="HorizontalCanvas_MouseWheel">
                <Canvas x:Name="RecentGamesContainer"/>
            </Canvas>
        </StackPanel>

        <!-- User Games (vertical multi-row grid) -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition x:Name="UserGamesCanvasRow" Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="32,16,32,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Path x:Name="UserGamesToggle"
                          Data="M 0,0 H 10 V 10 H 0 Z M 2,5 H 8 M 5,2 V 8"
                          Stroke="White" StrokeThickness="1.3" Fill="Transparent"
                          Width="12" Height="12" Stretch="Uniform"
                          Opacity="0.5" Cursor="Hand" VerticalAlignment="Center" Margin="0,1,10,0"
                          MouseLeftButtonUp="UserGamesToggle_Click"/>
                    <TextBlock Text="Installed" x:Name="UserGamesLabel" Foreground="White" Opacity="1"
                               FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"
                               Cursor="Hand" MouseLeftButtonUp="UserGamesLabel_Click"/>
                </StackPanel>
                <Popup Grid.Column="0" x:Name="UserGamesDropdown"
                       PlacementTarget="{Binding ElementName=UserGamesLabel}"
                       Placement="Bottom" StaysOpen="False" AllowsTransparency="True"
                       Closed="UserGamesDropdown_Closed">
                    <Border Margin="12" Background="#FF282C2E" BorderBrush="#FF404448" BorderThickness="1" MinWidth="160">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" ShadowDepth="4" BlurRadius="12" Opacity="0.8"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock x:Name="FilterOptionInstalled" Style="{StaticResource DropdownItemStyle}" Text="Installed" Tag="Installed" MouseLeftButtonUp="UserGamesFilter_Click"/>
                            <TextBlock x:Name="FilterOptionRemoved" Style="{StaticResource DropdownItemStyle}" Text="Removed" Tag="Removed" MouseLeftButtonUp="UserGamesFilter_Click"/>
                            <TextBlock x:Name="FilterOptionFinished" Style="{StaticResource DropdownItemStyle}" Text="Finished" Tag="Finished" MouseLeftButtonUp="UserGamesFilter_Click"/>
                            <TextBlock x:Name="FilterOptionUnfinished" Style="{StaticResource DropdownItemStyle}" Text="Unfinished" Tag="Unfinished" MouseLeftButtonUp="UserGamesFilter_Click"/>
                            <TextBlock x:Name="FilterOptionAll" Style="{StaticResource DropdownItemStyle}" Text="All" Tag="All" MouseLeftButtonUp="UserGamesFilter_Click"/>
                        </StackPanel>
                    </Border>
                </Popup>
                <TextBlock Grid.Column="1" Text="" x:Name="UserGamesCount" Foreground="White" Opacity="1"
                           FontSize="16" FontWeight="Normal"
                           VerticalAlignment="Center" Margin="8,0,8,0"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                    <TextBlock x:Name="UserGamesOrderDirectionToggle" Text="▲" Foreground="White" Opacity="0.5"
                               FontSize="16" Cursor="Hand" VerticalAlignment="Center" Margin="0,0,8,0"
                               MouseLeftButtonUp="UserGamesOrderDirectionToggle_Click"/>
                    <TextBlock x:Name="UserGamesOrderLabel" Text="Title" Foreground="White" Opacity="1"
                               FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"
                               Cursor="Hand" MouseLeftButtonUp="UserGamesOrderLabel_Click"/>
                </StackPanel>
                <Popup Grid.Column="2" x:Name="UserGamesOrderDropdown"
                       PlacementTarget="{Binding ElementName=UserGamesOrderLabel}"
                       Placement="Bottom" StaysOpen="False" AllowsTransparency="True"
                       Closed="UserGamesOrderDropdown_Closed">
                    <Border Margin="12" Background="#FF282C2E" BorderBrush="#FF404448" BorderThickness="1" MinWidth="160">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" ShadowDepth="4" BlurRadius="12" Opacity="0.8"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock x:Name="SortOptionTitle" Style="{StaticResource DropdownItemStyle}" Text="Title" Tag="Title" MouseLeftButtonUp="UserGamesOrder_Click"/>
                            <TextBlock x:Name="SortOptionAdded" Style="{StaticResource DropdownItemStyle}" Text="Added" Tag="Added" MouseLeftButtonUp="UserGamesOrder_Click"/>
                            <TextBlock x:Name="SortOptionFinished" Style="{StaticResource DropdownItemStyle}" Text="Finished" Tag="Finished" MouseLeftButtonUp="UserGamesOrder_Click"/>
                            <TextBlock x:Name="SortOptionPlayed" Style="{StaticResource DropdownItemStyle}" Text="Played" Tag="Played" MouseLeftButtonUp="UserGamesOrder_Click"/>
                            <TextBlock x:Name="SortOptionRating" Style="{StaticResource DropdownItemStyle}" Text="Rating" Tag="Rating" MouseLeftButtonUp="UserGamesOrder_Click"/>
                        </StackPanel>
                    </Border>
                </Popup>
                <Rectangle Grid.Column="3" x:Name="UserGamesDivider" Height="1" Fill="White" Opacity="0.2" VerticalAlignment="Center" Visibility="Collapsed" Margin="0,3,0,0"/>
                <Rectangle Grid.Column="3" x:Name="UserGamesScrollTrack" Height="1" Fill="White" Opacity="0.2" VerticalAlignment="Center" Margin="0,3,0,0"/>
                <Rectangle Grid.Column="3" x:Name="UserGamesScrollThumb" Height="3" Fill="LightSkyBlue" Opacity="0" VerticalAlignment="Center" Margin="0,3,0,0" HorizontalAlignment="Left" Width="0">
                    <Rectangle.Effect>
                        <DropShadowEffect Color="LightSkyBlue" ShadowDepth="0" BlurRadius="6" Opacity="0.8"/>
                    </Rectangle.Effect>
                </Rectangle>
            </Grid>
            <Canvas Grid.Row="1" x:Name="UserGamesCanvas" ClipToBounds="True" Margin="0,0,0,0" MouseWheel="VerticalCanvas_MouseWheel">
                <Canvas x:Name="UserGamesContainer"/>
            </Canvas>
        </Grid>

    </Grid>
</Window>

