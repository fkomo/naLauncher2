<Window x:Class="naLauncher2.Wpf.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:naLauncher2.Wpf"
    mc:Ignorable="d"
    Title="MainWindow"
    WindowStyle="None"
    WindowState="Maximized"
    AllowsTransparency="True"
    Background="#00202020"
    KeyDown="Window_KeyDown"
    Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="DropdownItemStyle" TargetType="TextBlock">
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3A3F42"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid x:Name="RootGrid" Background="#EE181D1F">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- New Games (horizontal strip) -->
        <StackPanel Grid.Row="0" Orientation="Vertical">
            <Grid Margin="32,32,32,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="New" x:Name="NewGamesLabel" Foreground="White" Opacity="1"
                           FontSize="16" FontWeight="SemiBold"
                           VerticalAlignment="Center" Margin="0,0,0,0"/>
                <TextBlock Grid.Column="1" Text="" x:Name="NewGamesCount" Foreground="White" Opacity="1"
                           FontSize="16" FontWeight="Normal"
                           VerticalAlignment="Center" Margin="8,0,16,0"/>
                <Rectangle Grid.Column="2" x:Name="NewGamesScrollTrack" Height="8" Fill="White" Opacity="0.2" VerticalAlignment="Center" Margin="0,3,0,0"/>
                <Rectangle Grid.Column="2" x:Name="NewGamesScrollThumb" Height="8" Fill="LightSkyBlue" Opacity="0.8" VerticalAlignment="Center" Margin="0,3,0,0" HorizontalAlignment="Left" Width="0">
                    <Rectangle.Effect>
                        <DropShadowEffect Color="LightSkyBlue" ShadowDepth="0" BlurRadius="6" Opacity="0.8"/>
                    </Rectangle.Effect>
                </Rectangle>
            </Grid>
            <Canvas x:Name="NewGamesCanvas" Height="292" ClipToBounds="True" Margin="0,0,0,0" MouseWheel="HorizontalCanvas_MouseWheel">
                <Canvas x:Name="NewGamesContainer"/>
            </Canvas>
        </StackPanel>

        <!-- Recent Games (horizontal strip) -->
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <Grid Margin="32,16,32,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Recent" x:Name="RecentGamesLabel" Foreground="White" Opacity="1"
                           FontSize="16" FontWeight="SemiBold"
                           VerticalAlignment="Center" Margin="0,0,0,0"/>
                <TextBlock Grid.Column="1" Text="" x:Name="RecentGamesCount" Foreground="White" Opacity="1"
                           FontSize="16" FontWeight="Normal"
                           VerticalAlignment="Center" Margin="8,0,16,0"/>
                <Rectangle Grid.Column="2" x:Name="RecentGamesScrollTrack" Height="8" Fill="White" Opacity="0.2" VerticalAlignment="Center" Margin="0,3,0,0"/>
                <Rectangle Grid.Column="2" x:Name="RecentGamesScrollThumb" Height="8" Fill="LightSkyBlue" Opacity="0.8" VerticalAlignment="Center" Margin="0,3,0,0" HorizontalAlignment="Left" Width="0">
                    <Rectangle.Effect>
                        <DropShadowEffect Color="LightSkyBlue" ShadowDepth="0" BlurRadius="6" Opacity="0.8"/>
                    </Rectangle.Effect>
                </Rectangle>
            </Grid>
            <Canvas x:Name="RecentGamesCanvas" Height="292" ClipToBounds="True" Margin="0,0,0,0" MouseWheel="HorizontalCanvas_MouseWheel">
                <Canvas x:Name="RecentGamesContainer"/>
            </Canvas>
        </StackPanel>

        <!-- User Games (vertical multi-row grid) -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="32,16,32,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Installed" x:Name="UserGamesLabel" Foreground="White" Opacity="1"
                           FontSize="16" FontWeight="SemiBold"
                           VerticalAlignment="Center" Margin="0,0,0,0"
                           Cursor="Hand" MouseLeftButtonUp="UserGamesLabel_Click"/>
                <Popup Grid.Column="0" x:Name="UserGamesDropdown"
                       PlacementTarget="{Binding ElementName=UserGamesLabel}"
                       Placement="Bottom" StaysOpen="False" AllowsTransparency="True"
                       Closed="UserGamesDropdown_Closed">
                    <Border Margin="12" Background="#FF282C2E" BorderBrush="#FF404448" BorderThickness="1" MinWidth="160">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" ShadowDepth="4" BlurRadius="12" Opacity="0.8"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock x:Name="FilterOptionInstalled" Style="{StaticResource DropdownItemStyle}" Text="Installed" Tag="Installed" MouseLeftButtonUp="UserGamesFilter_Click"/>
                            <TextBlock x:Name="FilterOptionRemoved" Style="{StaticResource DropdownItemStyle}" Text="Removed" Tag="Removed" MouseLeftButtonUp="UserGamesFilter_Click"/>
                            <TextBlock x:Name="FilterOptionFinished" Style="{StaticResource DropdownItemStyle}" Text="Finished" Tag="Finished" MouseLeftButtonUp="UserGamesFilter_Click"/>
                            <TextBlock x:Name="FilterOptionUnfinished" Style="{StaticResource DropdownItemStyle}" Text="Unfinished" Tag="Unfinished" MouseLeftButtonUp="UserGamesFilter_Click"/>
                            <TextBlock x:Name="FilterOptionAll" Style="{StaticResource DropdownItemStyle}" Text="All" Tag="All" MouseLeftButtonUp="UserGamesFilter_Click"/>
                        </StackPanel>
                    </Border>
                </Popup>
                <TextBlock Grid.Column="1" Text="" x:Name="UserGamesCount" Foreground="White" Opacity="1"
                           FontSize="16" FontWeight="Normal"
                           VerticalAlignment="Center" Margin="8,0,16,0"/>
                <Rectangle Grid.Column="2" x:Name="UserGamesScrollTrack" Height="8" Fill="White" Opacity="0.2" VerticalAlignment="Center" Margin="0,3,0,0"/>
                <Rectangle Grid.Column="2" x:Name="UserGamesScrollThumb" Height="8" Fill="LightSkyBlue" Opacity="0.8" VerticalAlignment="Center" Margin="0,3,0,0" HorizontalAlignment="Left" Width="0">
                    <Rectangle.Effect>
                        <DropShadowEffect Color="LightSkyBlue" ShadowDepth="0" BlurRadius="6" Opacity="0.8"/>
                    </Rectangle.Effect>
                </Rectangle>
            </Grid>
            <Canvas Grid.Row="1" x:Name="UserGamesCanvas" ClipToBounds="True" Margin="0,0,0,0" MouseWheel="VerticalCanvas_MouseWheel">
                <Canvas x:Name="UserGamesContainer"/>
            </Canvas>
        </Grid>

    </Grid>
</Window>

